import * as THREE from 'three';
import { OrbitControls } from 'three/addons/controls/OrbitControls.js';
import { byId as linenById } from '../data/linens.js';
let renderer,scene,camera,controls,root,container,ro,raf,state,night=false,ambient,hemi,sun,callbacks={},raycaster,pointer,drag=null,danceMesh=null,needsRender=true;
const renderedItems=new Map(),EAVE=7,M={},GROUND=new THREE.Plane(new THREE.Vector3(0,1,0),0);
function invalidate(){needsRender=true;}
function mat(k,o){return M[k]||(M[k]=new THREE.MeshStandardMaterial(o));}
function box(w,h,d,m){return new THREE.Mesh(new THREE.BoxGeometry(w,h,d),m);}
function cyl(r,h,m,n=8){return new THREE.Mesh(new THREE.CylinderGeometry(r,r,h,n),m);}
function tube(a,b,r,m){const d=new THREE.Vector3().subVectors(b,a),q=cyl(r,d.length(),m,6);q.position.copy(a).add(b).multiplyScalar(.5);q.quaternion.setFromUnitVectors(new THREE.Vector3(0,1,0),d.normalize());return q;}
const steel=()=>mat('steel',{color:0xbcc1c4,roughness:.3,metalness:.65}),dark=()=>mat('dark',{color:0x303437,roughness:.5}),strap=()=>mat('strap',{color:0xe4e0d4,roughness:.85});
const COLORS={'White':0xf8f7f2,'Ivory':0xf0e4c9,'Champagne':0xd9c29d,'Gold':0xb88b3b,'Black':0x17191c,'Silver':0xa9afb5,'Navy Blue':0x162b52,'Royal Blue':0x1e4da1,'Dusty Blue':0x7895ad,'Burgundy':0x641d2b,'Red':0xa52527,'Blush':0xe5b9b5,'Dusty Rose':0xb87979,'Pink':0xe5a7bc,'Purple':0x76518e,'Sage Green':0x8b9b78,'Hunter Emerald Green':0x285d48};
function colorFor(n,f=0xf8f7f2){return COLORS[n]||f;}
function H(t){if(t.type!=='pole')return EAVE+Math.max(4,t.widthFt*.24);if(t.widthFt<=20)return EAVE+7.5;if(t.widthFt<30)return EAVE+8.5;return EAVE+10.5;}
function peaks(t){const L=t.lengthFt,a=(t.centerPoles||[]).map(p=>({x:(p.x??t.widthFt/2)-t.widthFt/2,z:(p.y??L/2)-L/2}));return a.length?a:[{x:0,z:0}];}
function perimeter(t){const hw=t.widthFt/2,hl=t.lengthFt/2,p=[];for(let x=-hw;x<=hw+.01;x+=10)p.push([Math.min(x,hw),-hl],[Math.min(x,hw),hl]);for(let z=-hl+10;z<hl;z+=10)p.push([-hw,z],[hw,z]);return p;}
function nearestPeak(ps,x,z){let b=ps[0],d=Infinity;for(const p of ps){const n=(x-p.x)**2+(z-p.z)**2;if(n<d){d=n;b=p;}}return b;}
function membraneHeight(t,x,z){const W=t.widthFt,L=t.lengthFt,h=H(t),hw=W/2,hl=L/2,ps=peaks(t);if(t.type!=='pole')return EAVE+(h-EAVE)*Math.pow(Math.max(0,1-Math.abs(x)/hw),1.1);const zs=ps.map(q=>q.z).sort((a,b)=>a-b);let y=EAVE;ps.forEach(p=>{const dx=Math.abs(x-p.x)/hw;const i=zs.indexOf(p.z);let span=hl;if(zs.length>1){const l=i?Math.abs(p.z-zs[i-1])/2:Math.abs(p.z+hl),r2=i<zs.length-1?Math.abs(zs[i+1]-p.z)/2:Math.abs(hl-p.z);span=Math.max(8,Math.max(l,r2));}const dz=Math.abs(z-p.z)/span,r=Math.min(1,Math.sqrt(dx*dx+dz*dz)),cand=EAVE+(h-EAVE)*Math.pow(Math.max(0,1-r),1.32);if(cand>y)y=cand;});const edge=Math.min(hw-Math.abs(x),hl-Math.abs(z));if(edge<=.04)return EAVE;if(edge<1.2)y=EAVE+(y-EAVE)*(edge/1.2);return y;}
function roof(t){const W=t.widthFt,L=t.lengthFt,g=new THREE.PlaneGeometry(W,L,Math.max(24,Math.round(W)),Math.max(30,Math.round(L))),a=g.attributes.position;for(let i=0;i<a.count;i++)a.setZ(i,membraneHeight(t,a.getX(i),a.getY(i)));g.rotateX(-Math.PI/2);g.computeVertexNormals();const q=new THREE.Mesh(g,new THREE.MeshStandardMaterial({color:0xfffefa,roughness:.68,side:THREE.DoubleSide}));q.castShadow=true;q.receiveShadow=true;return q;}
function tent(t,anchor){const g=new THREE.Group(),hw=t.widthFt/2,hl=t.lengthFt/2,h=H(t),pp=perimeter(t),vm=mat('val',{color:0xfffefa,roughness:.76});g.add(roof(t));[[0,-hl,t.widthFt,.05],[0,hl,t.widthFt,.05],[-hw,0,.05,t.lengthFt],[hw,0,.05,t.lengthFt]].forEach(v=>{const q=box(v[2],.38,v[3],vm);q.position.set(v[0],EAVE-.19,v[1]);g.add(q);});pp.forEach(([x,z])=>{const p=cyl(.035,EAVE,steel());p.position.set(x,EAVE/2,z);g.add(p);});if(t.type==='pole')peaks(t).forEach(p=>{const q=cyl(.05,h+.6,steel());q.position.set(p.x,(h+.6)/2,p.z);g.add(q);});const c=t.installationClearanceFt||5;pp.forEach(([x,z])=>{const ex=Math.abs(x)>hw-.1,ez=Math.abs(z)>hl-.1,ox=x+(ex?Math.sign(x)*c:0),oz=z+(ez?Math.sign(z)*c:0);if(anchor==='stake'){const a=new THREE.Vector3(x,EAVE-.08,z),b=new THREE.Vector3(ox,.08,oz);g.add(tube(a,b,.012,strap()));}});return g;}
function chair(){const g=new THREE.Group(),m=mat('chair',{color:0xf7f7f2,roughness:.55}),s=box(1.05,.08,.95,m);s.position.y=.88;g.add(s);const b=box(1.03,.9,.055,m);b.position.set(0,1.48,-.43);g.add(b);return g;}
function placeSetting(g,x,z,a,c){const plate=cyl(.3,.025,mat('plate',{color:0xfafafa,roughness:.35}),12);plate.position.set(x,2.5,z);g.add(plate);const nap=box(.2,.025,.34,new THREE.MeshStandardMaterial({color:c,roughness:.9}));nap.position.set(x,2.53,z);nap.rotation.y=-a;g.add(nap);}
function table(o){const g=new THREE.Group(),w=o.widthFt||5,d=o.depthFt||5,h=2.45,round=o.shape==='round'||Math.abs(w-d)<.2,linen=linenById(o.linenId),lc=colorFor(o.linenColor||o.color||'White'),tm=new THREE.MeshStandardMaterial({color:linen?lc:0xb88d61,roughness:linen?.88:.58});if(round){const r=w/2,skirt=new THREE.Mesh(new THREE.CylinderGeometry(r,r*.94,h,24,1,true),tm);skirt.position.y=h/2;g.add(skirt);const top=cyl(r,.06,tm,24);top.position.y=h;g.add(top);}else{const q=box(w,linen?h:.16,d,tm);q.position.y=linen?h/2:h;g.add(q);}const n=o.seatCount||0,rr=Math.max(w,d)/2+1,nc=colorFor(o.napkinColor||o.linenColor||'White');for(let i=0;i<n;i++){const a=i/n*Math.PI*2,c=chair();c.position.set(Math.cos(a)*rr,0,Math.sin(a)*rr);c.rotation.y=-a-Math.PI/2;g.add(c);if(round)placeSetting(g,Math.cos(a)*(w*.31),Math.sin(a)*(w*.31),a,nc);}g.userData={itemId:o.id,kind:'table'};g.traverse(m=>m.userData.itemId=o.id);return g;}
function danceGroup(items,t){const g=new THREE.Group();if(!items.length)return g;let minX=Infinity,minY=Infinity,maxX=-Infinity,maxY=-Infinity;items.forEach(o=>{minX=Math.min(minX,o.x);minY=Math.min(minY,o.y);maxX=Math.max(maxX,o.x+o.widthFt);maxY=Math.max(maxY,o.y+o.depthFt);});const w=maxX-minX,d=maxY-minY,b=box(w,.08,d,dark());b.position.y=.04;g.add(b);const wm=mat('wood',{color:0xb57c45,roughness:.5});for(let x=-w/2;x<w/2;x+=3)for(let z=-d/2;z<d/2;z+=3){const pw=Math.min(3,w-(x+w/2)),pd=Math.min(3,d-(z+d/2)),p=box(pw-.03,.045,pd-.03,wm);p.position.set(x+pw/2,.105,z+pd/2);g.add(p);}g.position.set((minX+maxX)/2-t.widthFt/2,0,(minY+maxY)/2-t.lengthFt/2);g.userData={kind:'danceGroup',itemIds:items.map(o=>o.id)};g.traverse(m=>{m.userData.kind='danceGroup';m.userData.itemIds=g.userData.itemIds;});return g;}
function ground(t){const p=new THREE.Mesh(new THREE.PlaneGeometry(t.widthFt+100,t.lengthFt+100),mat('ground',{color:0x829d68,roughness:1}));p.rotation.x=-Math.PI/2;p.receiveShadow=true;return p;}
function clear(){while(root&&root.children.length)root.remove(root.children[0]);renderedItems.clear();danceMesh=null;}
function rebuild(d){state=d;clear();const t=d?.tent;if(!t)return;root.add(ground(t),tent(t,d.anchoringMethod));const hw=t.widthFt/2,hl=t.lengthFt/2,dance=[];(d.objects||[]).forEach(o=>{if(o.kind==='dance'){dance.push(o);return;}if(o.kind!=='table')return;const q=table(o);q.position.set(o.x+o.widthFt/2-hw,0,o.y+o.depthFt/2-hl);renderedItems.set(o.id,q);root.add(q);});if(dance.length){danceMesh=danceGroup(dance,t);root.add(danceMesh);}apply();invalidate();}
function apply(){if(!scene)return;scene.background=new THREE.Color(night?0x18263b:0xe7eef1);scene.fog.color.copy(scene.background);ambient.intensity=night?0.2:0.58;hemi.intensity=night?0.32:1.15;sun.intensity=night?0.08:2.1;renderer.toneMappingExposure=night?0.9:1.05;invalidate();}
function pointerRay(e){const r=renderer.domElement.getBoundingClientRect();pointer.x=((e.clientX-r.left)/r.width)*2-1;pointer.y=-((e.clientY-r.top)/r.height)*2+1;raycaster.setFromCamera(pointer,camera);return raycaster;}
function groundPoint(e){const out=new THREE.Vector3();return pointerRay(e).ray.intersectPlane(GROUND,out)?out:null;}
function hit(e){pointerRay(e);return raycaster.intersectObjects(root.children,true).find(h=>h.object.userData.itemId||h.object.userData.kind==='danceGroup');}
function down(e){const h=hit(e);if(!h)return;const ud=h.object.userData,p=groundPoint(e);if(!p)return;if(ud.kind==='danceGroup'){const ids=ud.itemIds||[],objs=state.objects.filter(o=>ids.includes(o.id));if(!objs.length)return;drag={kind:'dance',ids,start:p.clone(),orig:objs.map(o=>({id:o.id,x:o.x,y:o.y})),meshStart:danceMesh?.position.clone()};callbacks.onSelect?.(ids[0]);}else if(ud.itemId){const o=state.objects.find(x=>x.id===ud.itemId);if(!o)return;drag={kind:'item',id:ud.itemId,start:p.clone(),orig:{...o}};callbacks.onSelect?.(ud.itemId);}if(drag){controls.enabled=false;renderer.domElement.style.cursor='grabbing';renderer.domElement.setPointerCapture?.(e.pointerId);e.preventDefault();}}
function move(e){if(!drag)return;const p=groundPoint(e);if(!p)return;const t=state.tent,dx=p.x-drag.start.x,dz=p.z-drag.start.z;if(drag.kind==='dance'){const orig=drag.orig,objs=state.objects.filter(o=>drag.ids.includes(o.id)),minX=Math.min(...orig.map(o=>o.x)),minY=Math.min(...orig.map(o=>o.y)),maxX=Math.max(...orig.map(o=>{const live=objs.find(x=>x.id===o.id);return o.x+(live?.widthFt||0);})),maxY=Math.max(...orig.map(o=>{const live=objs.find(x=>x.id===o.id);return o.y+(live?.depthFt||0);})),cx=Math.max(-minX,Math.min(t.widthFt-maxX,dx)),cy=Math.max(-minY,Math.min(t.lengthFt-maxY,dz));orig.forEach(o=>{const live=state.objects.find(x=>x.id===o.id);if(live){live.x=o.x+cx;live.y=o.y+cy;}});if(danceMesh&&drag.meshStart)danceMesh.position.set(drag.meshStart.x+cx,drag.meshStart.y,drag.meshStart.z+cy);}else{const o=drag.orig,live=state.objects.find(x=>x.id===drag.id);if(live){live.x=Math.max(0,Math.min(t.widthFt-o.widthFt,o.x+dx));live.y=Math.max(0,Math.min(t.lengthFt-o.depthFt,o.y+dz));const mesh=renderedItems.get(live.id);if(mesh)mesh.position.set(live.x+live.widthFt/2-t.widthFt/2,0,live.y+live.depthFt/2-t.lengthFt/2);}}invalidate();e.preventDefault();}
function up(e){if(!drag)return;if(drag.kind==='dance')drag.ids.forEach(id=>{const o=state.objects.find(x=>x.id===id);if(o)callbacks.onMove?.(id,o.x,o.y);});else{const o=state.objects.find(x=>x.id===drag.id);if(o)callbacks.onMove?.(o.id,o.x,o.y);}drag=null;controls.enabled=true;renderer.domElement.style.cursor='grab';try{renderer.domElement.releasePointerCapture?.(e.pointerId);}catch(_){}invalidate();}
function frame(t){const h=H(t),hw=t.widthFt/2,hl=t.lengthFt/2,radius=Math.sqrt(hw*hw+hl*hl+h*h)*1.05,fov=(camera.fov||38)*Math.PI/180,d=Math.max(radius/Math.sin(fov/2),Math.max(t.widthFt,t.lengthFt)*.9);camera.position.set(d*.62,d*.34,d*.72);controls.target.set(0,Math.min(h*.4,6),0);controls.update();invalidate();}
function sizeCanvas(){if(!renderer||!container)return;const w=Math.max(320,Math.round(container.clientWidth)),h=Math.max(360,Math.round(container.clientHeight));camera.aspect=w/h;camera.updateProjectionMatrix();renderer.setSize(w,h,true);renderer.domElement.style.width='100%';renderer.domElement.style.height='100%';invalidate();}
function loop(){raf=requestAnimationFrame(loop);controls?.update();if(needsRender){renderer?.render(scene,camera);needsRender=false;}}
export function mount(el,data,cbs={}){unmount();callbacks=cbs;container=el;container.innerHTML='';night=false;scene=new THREE.Scene();scene.fog=new THREE.Fog(0xe7eef1,150,420);root=new THREE.Group();scene.add(root);const w=Math.max(320,Math.round(el.clientWidth)),h=Math.max(360,Math.round(el.clientHeight));camera=new THREE.PerspectiveCamera(38,w/h,.1,1000);renderer=new THREE.WebGLRenderer({antialias:true,powerPreference:'high-performance'});const mobile=window.matchMedia&&window.matchMedia('(max-width: 768px)').matches;renderer.setPixelRatio(Math.min(window.devicePixelRatio||1,mobile?1.5:2));renderer.setSize(w,h,true);renderer.domElement.style.width='100%';renderer.domElement.style.height='100%';renderer.shadowMap.enabled=true;renderer.shadowMap.type=THREE.PCFShadowMap;renderer.outputColorSpace=THREE.SRGBColorSpace;renderer.toneMapping=THREE.ACESFilmicToneMapping;renderer.domElement.style.touchAction='none';renderer.domElement.style.cursor='grab';el.appendChild(renderer.domElement);controls=new OrbitControls(camera,renderer.domElement);controls.enableDamping=true;controls.dampingFactor=.09;controls.maxPolarAngle=Math.PI/2-.04;controls.addEventListener('change',invalidate);raycaster=new THREE.Raycaster();pointer=new THREE.Vector2();renderer.domElement.addEventListener('pointerdown',down);renderer.domElement.addEventListener('pointermove',move);renderer.domElement.addEventListener('pointerup',up);renderer.domElement.addEventListener('pointercancel',up);ambient=new THREE.AmbientLight(0xffffff,.58);hemi=new THREE.HemisphereLight(0xe9f4ff,0x50653d,1.15);sun=new THREE.DirectionalLight(0xfff4e4,2.1);const s=Math.max(data.tent.widthFt,data.tent.lengthFt);sun.position.set(s*.62,s*1.18,s*.5);sun.castShadow=true;sun.shadow.mapSize.set(1024,1024);sun.shadow.camera.left=sun.shadow.camera.bottom=-s;sun.shadow.camera.right=sun.shadow.camera.top=s;sun.shadow.camera.far=s*4;scene.add(ambient,hemi,sun);frame(data.tent);rebuild(data);ro=new ResizeObserver(sizeCanvas);ro.observe(el);loop();}
export function update(data){if(!scene)return;rebuild(data);}
export function focusItem(id){if(!state)return;const o=state.objects.find(x=>x.id===id);if(!o)return;const t=state.tent;controls.target.set(o.x+o.widthFt/2-t.widthFt/2,1.8,o.y+o.depthFt/2-t.lengthFt/2);camera.position.copy(controls.target).add(new THREE.Vector3(6,5,6));controls.update();invalidate();}
export function toggleDayNight(){night=!night;apply();return night;}
export function unmount(){if(raf)cancelAnimationFrame(raf);raf=0;ro?.disconnect();ro=null;if(renderer){renderer.domElement.removeEventListener('pointerdown',down);renderer.domElement.removeEventListener('pointermove',move);renderer.domElement.removeEventListener('pointerup',up);renderer.domElement.removeEventListener('pointercancel',up);}controls?.dispose();renderer?.dispose();if(container)container.innerHTML='';renderedItems.clear();danceMesh=null;renderer=scene=camera=controls=root=container=null;callbacks={};drag=null;}
